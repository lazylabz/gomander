---
import ProblemCard from "./components/ProblemCard.astro";
---
<section class="py-20 bg-white">
    <div class="container mx-auto px-6">
        <div class="text-center mb-16">
            <h2 class="text-4xl font-bold text-gray-900 mb-6">The Developer's Terminal Nightmare</h2>
            <p class="text-xl text-gray-600 max-w-3xl mx-auto">Every developer knows this pain. Modern apps require multiple services, and that means...</p>
        </div>

        <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-6 mb-12">
            <ProblemCard icon="🖥️" title="10+ Terminal Windows" description="Your desktop becomes a maze of terminal tabs" />
            <ProblemCard icon="😵" title="Lost in the Chaos" description="Which process crashed? Where's that error?" />
            <ProblemCard icon="⏰" title="Slow Team Onboarding" description="New devs spend hours figuring out what to run" />
            <ProblemCard icon="🔄" title="Repetitive Setup" description="Same commands, every single day" />
        </div>

        <div class="text-center">
            <p class="text-2xl font-semibold text-gray-900 mb-4">There has to be a better way.</p>
            <p class="text-lg text-gray-600">And there is. 👇</p>
        </div>
    </div>
</section>

<style>
    .problem-card {
        background: linear-gradient(135deg, #fef3c7, #fed7aa);
        border-left: 4px solid #f59e0b;
    }
</style>